<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #4F46E5;
        color: white;
        padding: 15px;
        text-align: center;
        border-radius: 5px 5px 0 0;
      }
      .content {
        padding: 20px;
        border: 1px solid #ddd;
        border-top: none;
        border-radius: 0 0 5px 5px;
      }
      .button {
        display: inline-block;
        background-color: #4F46E5;
        color: white;
        padding: 10px 20px;
        text-decoration: none;
        border-radius: 5px;
        margin: 20px 0;
      }
      .token {
        background-color: #f5f5f5;
        padding: 10px;
        border-radius: 5px;
        font-family: monospace;
        margin: 10px 0;
        text-align: center;
      }
      .footer {
        margin-top: 20px;
        font-size: 12px;
        color: #777;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1>Invitation to join a group</h1>
    </div>

    <div class="content">
      <p>Hello,</p>

      <p><strong><%= @sender.name %></strong> has invited you to join the group <strong><%= @group.name %></strong> on Gifters.</p>

      <% if @group.description.present? %>
        <p>Group description: <%= @group.description %></p>
      <% end %>

      <p>To join this group, you can click on the link below:</p>

      <p>
        <a href="<%= @invitation_url %>" class="button">Join the group</a>
      </p>

      <p>Or you can use this invitation code in the application:</p>

      <div class="token">
        <%= @token %>
      </div>

      <p>This invitation doesn't have an expiration date, but it can only be used once.</p>

      <p>See you soon on Gifters!</p>
    </div>

    <div class="footer">
      <p>This email was automatically sent by Gifters. Please do not reply to this message.</p>
    </div>
  </body>
</html>
